class Solution {
    public int findMin(int[] nums) {
        int x=0;
        int y=Integer.MAX_VALUE;
        for(int i=0;i<nums.length;i++)
        {
                if(nums[i]<y)
                {
                    y=nums[i];
                }   
        }
        return y;
        
    }
}
